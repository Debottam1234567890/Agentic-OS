SYSTEM ARCHITECTURE DIRECTIVE: Build and dynamically refactor a multi-file 3D N-Body Physics Engine in pure Python.

PHASE 1: SCAFFOLDING
Use `write_file` to generate the following architecture:
1. `vector.py`: A custom 3D Vector class with dunder methods for addition, subtraction, scalar multiplication, and magnitude.
2. `body.py`: A 'CelestialBody' class holding a position vector, velocity vector, and mass. Include a method `apply_force(force)`.
3. `engine.py`: A 'Simulation' class that holds a list of bodies. Include a `step(dt)` method that calculates gravitational attraction between all bodies using Newton's law of universal gravitation, updates velocities, and then updates positions.
4. `run.py`: Initializes 3 bodies in stable circular orbits and runs a 10-step simulation, printing the positions to the terminal.

PHASE 2: VERIFICATION
Use `execute_bash` to run `python3 run.py`. Verify that the simulation executes without crashing.

PHASE 3: THE DYNAMIC REFACOR (THE SURGICAL PATCH)
I now want to introduce a new physical law: 'Quantum Repulsion'. If two bodies get closer than 0.5 units, they must experience a massive repelling force to prevent collision.
- Do NOT rewrite any files completely.
- Use `map_architecture` to view the structure of the engine.
- Use `search_codebase` to locate the exact file and line number where gravitational attraction is calculated.
- Use `patch_file` to surgically inject the 'Quantum Repulsion' logic directly into the nested loop without modifying the rest of the file.

PHASE 4: FINAL EXECUTION
Use `execute_bash` to run `python3 run.py` again. Use `task_complete` to report the final output and verify that the patch was successful.